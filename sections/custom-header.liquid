<header class="custom-header">
  <div class="header-wrapper">

    <!-- MOBILE TOGGLE -->
    <div class="mobile-toggle" id="mobileToggle">☰</div>

    <!-- LOGO -->
    <div class="header-left">
      <a href="/">
        {% if section.settings.logo != blank %}
          <img src="{{ section.settings.logo | image_url }}" class="header-logo" alt="logo">
        {% else %}
          {{ shop.name }}
        {% endif %}
      </a>
    </div>

    <!-- CENTER CONTENT -->
    <div class="header-center">
      {% if section.settings.center_text != blank %}
        <p class="center-text">{{ section.settings.center_text }}</p>
      {% endif %}
    </div>

    <!-- BUTTON -->
    <div class="header-right">
      {% if section.settings.button_text != blank %}
        <a href="{{ section.settings.button_link }}" class="header-btn">
          <span>{{ section.settings.button_text }}</span>
          {% if section.settings.arrow_icon != blank %}
            <img src="{{ section.settings.arrow_icon | image_url }}" class="btn-arrow" alt="arrow">
          {% endif %}
        </a>
      {% endif %}
    </div>

  </div>
</header>

<!-- MOBILE DRAWER -->
<div class="mobile-menu" id="mobileMenu">

  <div class="mobile-menu-header">
    <span>Menu</span>
    <button id="closeMenu">✕</button>
  </div>

  {% if section.settings.center_text != blank %}
    <p class="mobile-center-text">{{ section.settings.center_text }}</p>
  {% endif %}

   <div class="mobile-menu-content">
  {% if section.settings.button_text != blank %}
    <a href="{{ section.settings.button_link }}" class="mobile-btn">
      <span>{{ section.settings.button_text }}</span>
      {% if section.settings.arrow_icon != blank %}
        <img src="{{ section.settings.arrow_icon | image_url }}" class="btn-arrow" alt="arrow">
      {% endif %}
    </a>
  {% endif %}
</div>

</div>

{% schema %}
{
  "name": "Custom Header",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "text",
      "id": "center_text",
      "label": "Center Text",
      "default": "Welcome to our store"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Contact Us"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "image_picker",
      "id": "arrow_icon",
      "label": "Arrow Icon"
    }
  ],
  "presets": [
    {
      "name": "Custom Header"
    }
  ]
}
{% endschema %}

<style>
.custom-header {
  position: sticky;
  top: 0;
  z-index: 999;
  background: #F5F5F5;
  padding: 15px 40px;
}

.header-wrapper {
  display: flex;
  align-items: center;
  justify-content: space-between;
  position: relative;
}

.header-logo {
  max-height: 40px;
}

/* CENTER DESKTOP */
.header-center {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  text-align: center;
}

.center-text {
  font-size: 14px;
}

/* BUTTON */
.header-btn {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 10px 20px;
  background: #FFF544;
  color: #000;
  text-decoration: none;
  position: relative;
  overflow: hidden;
}

.header-btn::before {
  content: "";
  position: absolute;
  width: 0%;
  height: 100%;
  background: #000;
  transition: 0.4s;
  z-index: -1;
}

.header-btn:hover::before {
  width: 100%;
}

.header-btn:hover {
  color: #fff;
}

.btn-arrow {
  width: 16px;
  filter: invert(1);
  transition: 0.3s;
}

.header-btn:hover .btn-arrow {
  transform: translateX(5px);
  filter: invert(0);
}

/* MOBILE */
.mobile-toggle {
  display: none;
  font-size: 24px;
  cursor: pointer;
}

/* FIX: mobile content center */
.mobile-menu-content {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 60vh;
}

/* FIX: proper mobile button */
.mobile-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  padding: 14px 25px;
  background: #FFF544;
  color: #000;
  text-decoration: none;
  font-size: 16px;
  position: relative;
  overflow: hidden;
  z-index: 1;
}

/* animation */
.mobile-btn::before {
  content: "";
  position: absolute;
  width: 0%;
  height: 100%;
  background: #000;
  left: 0;
  top: 0;
  transition: 0.4s;
  z-index: -1;
}

.mobile-btn:hover::before {
  width: 100%;
}

.mobile-btn:hover {
  color: #fff;
}

/* arrow fix */
.mobile-btn .btn-arrow {
  width: 16px;
  filter: invert(1);
  transition: 0.3s;
}

.mobile-btn:hover .btn-arrow {
  transform: translateX(5px);
  filter: invert(0);
}

/* RESPONSIVE */
@media (max-width: 768px) {

  .custom-header {
    padding: 12px 15px;
  }

  .mobile-toggle {
    display: block;
  }

  .header-left {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
  }

  .header-right {
    display: block;
  }

  .header-center {
    display: none;
  }

  .header-logo {
    max-height: 32px;
  }
}
</style>

<script>
document.addEventListener("DOMContentLoaded", function() {
  const openBtn = document.getElementById("mobileToggle");
  const closeBtn = document.getElementById("closeMenu");
  const menu = document.getElementById("mobileMenu");

  openBtn.addEventListener("click", () => {
    menu.classList.add("active");
  });

  closeBtn.addEventListener("click", () => {
    menu.classList.remove("active");
  });
});
</script>